apiVersion: v1
kind: Service
metadata:
  name: library-api
  namespace: sample-apis
  labels:
    app: library-api
spec:
  selector:
    app: library-api
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
