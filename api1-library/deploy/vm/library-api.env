# /etc/sample-apis/library-api.env
#
# Library API 환경변수 파일
# 이 파일은 root:svcapi 소유, 권한 640 으로 설정하세요.
#   sudo chown root:svcapi /etc/sample-apis/library-api.env
#   sudo chmod 640 /etc/sample-apis/library-api.env
#
PORT=3000
NODE_ENV=production
