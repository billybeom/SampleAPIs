# TLS edge termination — OpenShift Router가 HTTPS를 처리하고 Pod에는 HTTP로 전달
# host 값을 실제 클러스터 도메인으로 교체하세요.
#   예) odata-api.apps.your-cluster.example.com
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: odata-api
  namespace: sample-apis
  labels:
    app: odata-api
spec:
  host: odata-api.apps.your-cluster.example.com
  to:
    kind: Service
    name: odata-api
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
